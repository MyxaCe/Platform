[package]
name = "persistence"
version = "0.1.0"
edition = "2021"
description = "Durable-хранилище состояния платформы (счета брокера + реестр пользователей) за трейтом Persistence (ADR-016)"

[lib]
name = "persistence"
path = "src/lib.rs"

[dependencies]
domain = { path = "../domain" }
broker = { path = "../broker" }
async-trait = "0.1"
