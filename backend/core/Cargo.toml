[package]
name = "exchange-core"
version = "0.1.0"
edition = "2021"
description = "Детерминированное matching-ядро биржи: order book + matching engine"

[lib]
name = "exchange_core"
path = "src/lib.rs"

[dependencies]
domain = { path = "../domain" }
