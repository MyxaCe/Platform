[package]
name = "orchestrator"
version = "0.1.0"
edition = "2021"
description = "Путь ордера: резерв (ledger) → матчинг (core) → расчёт по событиям (ADR-008)"

[lib]
name = "orchestrator"
path = "src/lib.rs"

[dependencies]
domain = { path = "../domain" }
exchange-core = { path = "../core" }
ledger = { path = "../ledger" }
