---
tags: [moc, backlog]
updated: 2026-07-17
---

# Backlog — идеи и улучшения

Всё, что «потом»: улучшения, оптимизации, фичи, технический долг. Формат:

```markdown
- [ ] **Название** — суть, зачем, приоритет (P0/P1/P2). Ссылки: [[модуль]] / [[ADR-XXX]]
```

---

## Идеи

- [x] ~~**Снапшот стакана** — глубина/агрегированные уровни для market data и визуализации~~ ✅ сделано ([[order-book]])
- [x] ~~**Оркестратор Ledger↔Matching (Фаза 2b)** — резерв → матчинг → расчёт~~ ✅ сделано ([[orchestrator]], ADR-008)
- [x] ~~**Рыночные заявки в оркестраторе**~~ ✅ сделано (ADR-009, [[orchestrator]])
- [x] ~~**Предотвращение self-trade**~~ ✅ сделано, политика «отклонить входящую» (ADR-009)
- [ ] **Тонкие политики STP** — cancel-resting, decrement-and-cancel (сейчас только «отклонить входящую»). P2. [[orchestrator]]
- [ ] **Частичный market-buy по бюджету** — вместо отказа при недостатке средств. P2. [[orchestrator]]
- [ ] **Реестр активов** — заменить опаковый `AssetId` на полноценный (символ, точность). P1. [[instrument-registry]]
- [ ] **Больше типов заявок** — FOK, post-only, stop/stop-limit. P2. [[matching-engine]]
- [ ] **Статусы инструмента** — trading/halt/auction; лимиты цены-объёма. P2. [[instrument-registry]]
- [ ] **Свойственные (property-based) тесты** — сохранение объёма при матчинге, сортировка книги. P1.
- [ ] **Оптимизация отмены** — `cancel` линейно ищет внутри ценового уровня. P2. [[order-book]]
- [ ] **Метрики матчинга** — латентность, длина очереди, глубина стакана. P1.
- [ ] **Бенчмарк латентности** — прогон нагрузочного сценария. P2. [[matching-engine]]

## Gateway (сетевой слой)

- [ ] **Настоящий auth** — вместо dev-токенов: хэши паролей, сессии/JWT, ротация, роли. P0 для прод. [[gateway]]
- [ ] **Защита admin-эндпоинтов** — admin-ключ/роль (сейчас открыты, dev). P0. [[gateway]]
- [ ] **Суммы строками в JSON** — большие `i128` теряют точность в JS-клиентах. P1. [[gateway]]
- [ ] **WS-подписки** — по инструменту и приватные события пользователя (сейчас всем всё). P1. [[gateway]]
- [ ] **Single-writer через actor/канал** — вместо `Mutex` выделенный поток оркестратора. P2. [[gateway]]

## Реальные данные и broker-режим (ADR-013)

- [x] ~~**Реальные крипто-данные**~~ ✅ Binance топ-30 (свечи/цены/лента), симуляция убрана
- [x] ~~**Broker-исполнение** — сделки/позиции/маржа/P&L на реальных ценах~~ ✅ бумажный [[broker]] (ADR-014)
- [x] ~~**Стоп-лосс / тейк-профит**~~ ✅ на позициях + монитор-триггер (ADR-014)
- [x] ~~**Лимитные ордера**~~ ✅ отложенные ордера с триггером по цене
- [x] ~~**Closed deals**~~ ✅ история закрытых сделок
- [ ] **Плечо/ликвидация/margin-call** — сейчас leverage=1, ликвидации нет. P1. [[broker]]
- [ ] **Комиссии, свопы (funding)** — брокерская обвязка. P2. [[broker]]
- [ ] **Персистентность позиций/счетов/ордеров** — сейчас в памяти. P1. [[broker]]
- [ ] **SIGNALS — доработка** (в отладке): паттерн-движок вместо простых RSI/MA-крест. Autochartist недоступен (проприетарный). P3. [[web-ui]]
- [ ] **Индикаторы: Ichimoku/Alligator со сдвигом, значения по курсору, серверный расчёт**. P2. [[web-ui]]
- [ ] **Индикаторы, требующие спец-рендеринга/данных** — Volume Profile, TPO, Delta (buy/sell тики), Fibonacci-разметка, ZigZag, Pivot Points. P2. [[web-ui]]
- [ ] **Не-крипто рынки** — форекс (major/minor/exotic), сырьё, акции EU/US/РФ, индексы: нужен платный
      дата-провайдер (Finnhub/Twelve Data/Polygon) + API-ключ; РФ осложнена санкциями. P1.
- [ ] **Плашка «данные с задержкой/источник»** и обработка недоступности Binance (451/офлайн). P2. [[gateway]]

## Web UI

- [x] ~~**Серверные свечи + история**~~ ✅ сделано (сначала [[marketdata]], теперь реальные Binance klines)
- [x] ~~**Инструменты из API**~~ ✅ сделано (`/instruments`, масштабы по decimals)
- [ ] **Персистентные свечи** — сейчас в памяти; durable-хранилище/журнал (Фаза 3). P1. [[marketdata]]
- [ ] **Метрики MARGIN/P&L** — заглушки; нужны позиции/маржа (за пределами спота). P2. [[web-ui]]
- [ ] **Мои открытые ордера/сделки** — вкладки внизу (нужен per-user эндпоинт). P2. [[gateway]]

## UI/UX — очередь на доработку (сессия 2026-07-18)

> **Статус 2026-07-18:** ✅ сделано почти всё (см. коммит «UI batch»): лоадер, выравнивание списка,
> полный Reset, убран MARKET TRADES, редизайн настроек + переключатели, toolbar-ресайз, крупнее иконки,
> BUG-006 (UTC), убран TV-лого, лого монет, индикаторы-тумблеры, английский, кастомные тултипы,
> сортировка по колонкам, **вкл/выкл колонок котировок** (в настройках ⚙, сетка перестраивается).
> **Осталось из этого списка:** ничего. Отдельно на будущее: ⬜ настоящие флаги стран — для будущих
> форекс/фиат-рынков (у крипты сейчас лого монет).

Список от владельца в конце сессии 2026-07-17. Приоритет P1, [[web-ui]].

- [ ] **Индикатор загрузки** при смене монеты / ТФ / типа графика. Сейчас во время `fetch /candles`
      интерфейс просто подвисает без обратной связи — выглядит плохо. Показывать спиннер/скелетон на
      графике (и, возможно, лёгкий оверлей) пока грузятся данные.
- [ ] **Список монет: выровнять данные** — числа разной длины дают «гребёнку». Колонки CHANGE/SELL/BUY/
      SPREAD/HIGH выровнять по ширине (tabular-nums + фиксированная/минимальная ширина) и **по левому краю**
      (сейчас по правому). Числа не должны «прыгать».
- [ ] **Reset графика = полный сброс отображения** (сейчас сбрасывает только зум). Должен также
      сбрасывать: тип свечей → Candles, все индикаторы (оверлеи + осциллятор), настройки отображения → дефолт.
- [ ] **Убрать MARKET TRADES** — вкладку и связанную логику (лента `aggTrade` в UI, `addTape`). WS-поток
      оставить для live-обновления цены графика; просто не показывать вкладку/ленту.
- [ ] **Настройки (⚙): переделать стиль + починить** — по словам владельца «выглядят ужасно и не работают».
      Сделать нормальную панель (сейчас `#setMenu` со `space-between` кривой). Проверить, что change-события
      реально применяются к серии: Hollow color, Candle shadows (вкл/выкл + цвет), Price line width,
      Additional price line, Minimal price change. Возможная причина «не работает»: color/number `change`
      не триггерятся как ожидается, либо меню закрывается при взаимодействии — проверить.
- [ ] **Растягивание toolbar** — уточнить у владельца точный смысл (высота тулбара? перенос на строки?
      горизонтальный скролл?). По аналогии с ресайзом панелей.
- [ ] **Иконки чуть крупнее** — сейчас `.ic svg` 15px; поднять до ~17-18px (и `.caret`, звёздочки пропорц.).

Добавлено владельцем в конце сессии (часть 2):

- [ ] **Убрать attribution-логотип TradingView на графике** (тот самый SVG-path `M14 2H2v6...`).
      Решение: `layout: { attributionLogo: false }` в `createChart` (lightweight-charts 4.1+; у нас 4.2 — ок).
- [ ] **Новый дизайн чекбоксов** — кастомные, в стиле терминала (сейчас нативные, выглядят чужеродно).
- [ ] **Indicators: убрать чекбоксы** — сделать toggle-подсветкой активного пункта (акцентный цвет при
      включении, как у Display PIPS), без нативных checkbox.
- [ ] **Убрать весь русский из UI** — i18n → английский везде: подписи, сообщения формы, тултипы, сигналы.
- [ ] **Кастомные тултипы** при наведении (вместо нативного `title`) — единый стиль терминала.
- [ ] **Флаги валют/монет в списке монет.** Нюанс: для фиата/форекса — флаг страны (EUR→EU, USD→US);
      для крипты страны нет → лого монеты, а для котируемой USDT можно флаг US. Нужен свой SVG-набор
      флагов/лого (как ICONS). Уточнить объём.
- [ ] **Настройка колонок котировок** — вкл/выкл CHANGE / SELL / BUY / SPREAD / HIGH (в настройках списка).
- [ ] **Сортировка списка монет** — клик по заголовку колонки (CHANGE/SELL/BUY/SPREAD/HIGH) → стрелка
      вверх/вниз, список сортируется по этому параметру (toggle asc/desc). Учесть текущий приоритет избранного.

## Технический долг

_Пока пусто._
