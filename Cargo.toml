# Cargo workspace — корень проекта.
# domain — общий словарь; core — matching-ядро; ledger — счета/балансы;
# orchestrator — путь ордера (резерв → матчинг → расчёт).
[workspace]
resolver = "2"
members = ["domain", "core", "ledger", "orchestrator"]
