# Cargo workspace — корень проекта.
# domain — общий словарь; core — matching-ядро; ledger — счета/балансы;
# orchestrator — путь ордера; gateway — сетевой шлюз (REST + WS).
[workspace]
resolver = "2"
members = ["domain", "core", "ledger", "orchestrator", "gateway"]
