[package]
name = "marketdata"
version = "0.1.0"
edition = "2021"
description = "Агрегация сделок в свечи (OHLCV) по таймфреймам (ADR-012)"

[lib]
name = "marketdata"
path = "src/lib.rs"

# Чистая логика, без зависимостей.
[dependencies]
